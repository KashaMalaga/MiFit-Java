package android.support.v4.app;

import android.app.Notification;
import android.os.Bundle;
import android.os.Parcelable;
import java.util.ArrayList;

interface C0044ba {
    Notification a(aT aTVar);

    Bundle a(Notification notification);

    Bundle a(bs bsVar);

    aM a(Notification notification, int i);

    bs a(Bundle bundle, bt btVar, cb cbVar);

    ArrayList<Parcelable> a(aM[] aMVarArr);

    aM[] a(ArrayList<Parcelable> arrayList);

    int b(Notification notification);

    String c(Notification notification);

    boolean d(Notification notification);

    String e(Notification notification);

    boolean f(Notification notification);

    String g(Notification notification);
}
