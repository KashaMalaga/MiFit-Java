package android.support.v4.app;

import android.app.PendingIntent;
import android.os.Bundle;

public interface br {
    bq b(int i, CharSequence charSequence, PendingIntent pendingIntent, Bundle bundle, ca[] caVarArr);

    bq[] b(int i);
}
