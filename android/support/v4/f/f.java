package android.support.v4.f;

import android.net.ConnectivityManager;

class f implements c {
    f() {
    }

    public boolean a(ConnectivityManager connectivityManager) {
        return i.a(connectivityManager);
    }
}
